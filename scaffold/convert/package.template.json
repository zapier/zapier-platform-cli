{
  "name": "<%= NAME %>",
  "version": "1.0.0",
  "description": "<%= DESCRIPTION %>",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive -t 10000"
  },
  "engines": {
    "node": "8.10.0",
    "npm": ">=5.6.0"
  },
  "dependencies": {
    "zapier-platform-core": "<%= CORE_VERSION %>",
    "zapier-platform-legacy-scripting-runner": "<%= RUNNER_VERSION %>"
  },
  "devDependencies": {
    "mocha": "^5.2.0",
    "should": "^13.2.0"
  },
  "private": true,
  "zapier": {
    "convertedFromAppID": <%= APP_ID %>,
    "convertedByCLIVersion": "<%= CLI_VERSION %>"
  }
}
