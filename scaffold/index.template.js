const App = {
  version: require('./package.json').version,
  platformVersion: require('@zapier/zapier-platform-core').version,

  resources: {
  },

  triggers: {
  },

  searches: {
  },

  writes: {
  },

};

module.exports = App;
