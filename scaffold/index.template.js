const App = {
  version: require('./package.json').version,
  platformVersion: require('zapier-platform-core').version,

  resources: {
  },

  triggers: {
  },

  searches: {
  },

  writes: {
  },

};

module.exports = App;
